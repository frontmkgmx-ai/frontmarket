rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /stores/{storeId}/{allPaths=**} {
      // Anyone can read images
      allow read: if true;
      // For the prototype, allow authenticated users to upload to their store path
      allow write: if request.auth != null;
    }
  }
}
